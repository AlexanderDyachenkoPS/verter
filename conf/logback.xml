<?xml version="1.0" encoding="UTF-8"?>
 
<configuration>
 
  <property name="LOG_FILE_NAME" value="verter"/>
  <property name="LOG_FILE_PATTERN" value="${LOG_FILE_NAME}"/>
 
 
<appender name="SIZE_AND_TIME_BASED_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>C:\UCELL\log\${LOG_FILE_PATTERN}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
                <fileNamePattern>C:\UCELL\log\${LOG_FILE_PATTERN}-%i.zip</fileNamePattern>
                <minIndex>1</minIndex>
                <maxIndex>10</maxIndex>       
        </rollingPolicy>
<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">

        <maxFileSize>10000KB</maxFileSize>

    </triggeringPolicy>

    <!-- encoders are  by default assigned the type
                           ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>
 
  <logger name="ru.billing.verter" level="DEBUG"/>
 
  <root level="DEBUG">
    <appender-ref ref="SIZE_AND_TIME_BASED_FILE" />
  </root>
 
</configuration>